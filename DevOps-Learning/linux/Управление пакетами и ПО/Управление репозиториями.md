## ![](https://ucarecdn.stepik.net/0db4bc91-c8ea-4dc7-b7d8-65c37111972a/)

В прошлых уроках вы познакомились с пакетными менеджерами и установкой программ из исходников. Теперь давайте разберёмся, откуда пакетные менеджеры получают программное обеспечение и как управлять источниками установки программ — **репозиториями**.

### Что такое репозиторий?

**Репозиторий** — это хранилище пакетов (программ и библиотек), доступных для установки в Linux-дистрибутивах. Репозитории могут быть официальными, поддерживаемыми разработчиками дистрибутива, и сторонними (неофициальными).

Управляя репозиториями, вы можете расширять доступный список программ или обновлять ПО до новых версий.

### Где хранятся настройки репозиториев?

Настройки репозиториев в популярных дистрибутивах Linux находятся по следующим путям:

- **Debian/Ubuntu**: файлы с расширением `.list` в директории:
    
    ```swift
    /etc/apt/sources.list
    /etc/apt/sources.list.d/
    ```
    
- **CentOS/Fedora/RHEL**: файлы с расширением `.repo` в директории:
- ```bash
    /etc/yum.repos.d/
    ```
    

> Для Debian/Ubuntu → основной файл `/etc/apt/sources.list` + дополнительные `.list` в `/etc/apt/sources.list.d/`.
> 
> Для CentOS/Fedora/RHEL → файлы `.repo` в `/etc/yum.repos.d/`.

Примеры: 

1. ```nginx
    deb http://archive.ubuntu.com/ubuntu focal main universe
    ```
    
2. ```ini
    [epel]
    name=Extra Packages for Enterprise Linux
    baseurl=https://download.fedoraproject.org/pub/epel/$releasever/Everything/$basearch
    enabled=1
    gpgcheck=1
    ```
    

### ![](https://ucarecdn.stepik.net/0bf68672-c35d-415b-b412-1af7393b8c68/)

### Добавление репозиториев в Debian/Ubuntu

Добавить репозиторий в Debian или Ubuntu можно двумя способами: вручную или с помощью команды `add-apt-repository`.

👉 После добавления PPA всегда выполняйте `sudo apt update`, чтобы обновить индекс пакетов. 

#### Добавление репозитория вручную

Отредактируйте файл `/etc/apt/sources.list` или создайте новый файл в `/etc/apt/sources.list.d/`:

```bash
sudo vim /etc/apt/sources.list.d/example.list
```

Добавьте строку в формате:

```nginx
deb http://адрес_репозитория/ имя_дистрибутива компоненты
```

Например:

```cpp
deb http://archive.ubuntu.com/ubuntu focal main restricted universe multiverse
```

После этого обновите список пакетов:

```bash
sudo apt update
```

#### Использование команды add-apt-repository

Эта команда позволяет добавлять репозитории просто и быстро:

```bash
sudo add-apt-repository ppa:graphics-drivers/ppa
sudo apt update
```

В данном примере добавляется популярный PPA-репозиторий с драйверами для графических карт.

### Удаление репозиториев в Debian/Ubuntu

Удалить репозиторий можно вручную, удалив или закомментировав строку в файле конфигурации, или командой:

```bash
sudo add-apt-repository --remove ppa:graphics-drivers/ppa
sudo apt update
```

### ![](https://ucarecdn.stepik.net/9d81c50b-7dcc-42f8-b746-fbe8c84b7d77/)

### Добавление репозиториев в CentOS/Fedora

Для добавления репозитория создайте новый файл в директории `/etc/yum.repos.d/` с расширением `.repo`. Например, добавим репозиторий EPEL (Extra Packages for Enterprise Linux):

```bash
sudo vim /etc/yum.repos.d/epel.repo
```

Добавьте следующее содержимое:

```ini
[epel]
name=Extra Packages for Enterprise Linux 8 - $basearch
baseurl=https://download.fedoraproject.org/pub/epel/8/Everything/$basearch
enabled=1
gpgcheck=1
gpgkey=https://dl.fedoraproject.org/pub/epel/RPM-GPG-KEY-EPEL-8
```

После этого обновите список пакетов:

```bash
sudo dnf update
```

### Включение и отключение репозиториев

Вы можете временно отключать репозитории, установив параметр `enabled=0` в файле конфигурации:

```ini
[example-repo]
name=Example Repo
baseurl=https://example.com/repo/
enabled=0
```

Чтобы временно включить отключённый репозиторий для одной команды, используйте:

```bash
sudo dnf install пакет --enablerepo=example-repo
```

👉 Чтобы снова включить — поменяйте на `enabled=1`

### Просмотр списка репозиториев

Чтобы увидеть список подключенных репозиториев:

- В Debian/Ubuntu:
    
    ```swift
    grep -rhE ^deb /etc/apt/sources.list /etc/apt/sources.list.d/
    ```
    
- В CentOS/Fedora:
    
    ```css
    dnf repolist all
    ```
    

### Полезные советы по управлению репозиториями

- Используйте только проверенные официальные репозитории или надёжные сторонние ресурсы, чтобы избежать проблем с безопасностью.
- Старайтесь не включать лишние репозитории без необходимости, так как это может привести к конфликтам версий пакетов.
- Регулярно проверяйте доступность и актуальность репозиториев, обновляя списки пакетов.

**Отлично!** Теперь вы можете управлять источниками программного обеспечения в своей Linux-системе и легко расширять её возможности.