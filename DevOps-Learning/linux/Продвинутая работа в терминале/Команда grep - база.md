## ![](https://ucarecdn.stepik.net/d4c3447d-a44c-4690-864f-e1b76a6c73f4/)

## Команда grep — базовый поиск

При работе в Linux часто приходится искать нужные строки или фрагменты текста в файлах или выводе других команд. Для этого существует мощная и широко используемая утилита — **grep**.

### Что такое grep?

**grep** расшифровывается как _"global regular expression print"_, то есть «глобальный вывод по регулярному выражению». Эта утилита помогает искать текстовые шаблоны в файлах и потоках данных.

Самое простое применение `grep` выглядит так:

```perl
grep "слово" файл.txt
```

Эта команда покажет все строки из файла `файл.txt`, в которых встречается слово `"слово"`.

### Простой поиск в файле

Допустим, у вас есть файл `notes.txt`:

```csharp
Linux is great
I love working in terminal
Linux commands are powerful
Windows vs Linux
```

Вы хотите найти строки, содержащие слово "Linux":

```perl
grep "Linux" notes.txt
```

Вывод будет следующим:

```csharp
Linux is great
Linux commands are powerful
Windows vs Linux
```

### ![](https://ucarecdn.stepik.net/37a15297-b74c-4d3c-9686-51ce97054589/)

### Поиск без учёта регистра

По умолчанию команда `grep` чувствительна к регистру. Чтобы найти слово независимо от регистра букв, используйте опцию `-i`:

```perl
grep -i "linux" notes.txt
```

Эта команда найдёт «Linux», «linux», «LINUX» и любые другие варианты регистра.

### Рекурсивный поиск по каталогам

Если нужно найти текст во всех файлах каталога и его подкаталогов, используйте ключ `-r`:

```perl
grep -r "error" /var/log
```

Эта команда найдёт все упоминания слова «error» во всех логах и подкаталогах `/var/log`.

### Вывод номеров строк и имён файлов

Дополнительные полезные опции:

- `-n` — отображает номера строк, где найдено совпадение.
- `-l` — выводит только имена файлов, где найдено совпадение.
- `-H` — всегда показывает имя файла перед строкой (полезно при поиске в нескольких файлах).

Пример использования:

```perl
grep -rnH "error" /var/log
```

Вывод может выглядеть так:

```lua
/var/log/syslog:245:Some error occurred
/var/log/auth.log:78:Authentication error
```

### Инвертированный поиск (поиск строк, НЕ содержащих шаблон)

Иногда нужно сделать наоборот — найти строки, которые НЕ содержат заданное слово или выражение. Для этого используйте ключ `-v`:

```perl
grep -v "Linux" notes.txt
```

Результат покажет все строки, где нет слова «Linux».

### Поиск в выводе других команд

Команду `grep` часто используют в связке с другими командами через конвейер (`|`). Например, вы хотите найти определённый процесс:

```perl
ps aux | grep nginx
```

Эта команда покажет все запущенные процессы, в названии которых встречается слово «nginx».

📌 **grep и бинарные файлы:**  
По умолчанию grep может выводить сообщения вроде «Binary file matches», если встречает бинарные данные. Чтобы заставить grep обрабатывать такие файлы как текстовые, используйте ключ **-a** или его длинную форму **--text**.

### Подсветка результатов поиска

Чтобы вывод результатов поиска был наглядным, можно использовать опцию `--color=auto`, которая подсвечивает найденные совпадения:

```cpp
grep --color=auto "Linux" notes.txt
```

Вывод будет иметь цветовое выделение слова «Linux».

Чтобы всегда использовать подсветку по умолчанию, можно создать псевдоним в файле `~/.bashrc`:

```bash
alias grep="grep --color=auto"
```

### Поиск целых слов

По умолчанию `grep` ищет указанный текст как часть большего слова. Если вы хотите найти только целое слово, используйте опцию `-w`:

```perl
grep -w "Linux" notes.txt
```

Эта команда найдёт только отдельные слова «Linux» и не найдёт «Linuxes» или «Linuxmint».

## Краткое напоминание основных опций grep:

- `-i` — игнорировать регистр букв.
- `-r` — рекурсивный поиск.
- `-n` — показать номера строк.
- `-l` — показать только имена файлов, содержащих совпадения.
- `-H` — всегда показывать имя файла.
- `-v` — инвертированный поиск (найти строки, не содержащие шаблон).
- `-w` — искать только целые слова.
- `--color=auto` — подсвечивать совпадения.

## Итог

Команда **grep** — один из самых полезных и мощных инструментов поиска текста в Linux. Она позволяет быстро находить нужную информацию в файлах или выводе других команд, а также фильтровать и обрабатывать данные в повседневной работе.

В следующем уроке мы закрепим эти знания на практике и подробнее изучим использование регулярных выражений с командой `grep`.

> 📌 **Переменные окружения и grep:**  
> На поведение grep влияют несколько переменных окружения. Самые важные:  
> – **GREP_OPTIONS** (устаревшая, но может встречаться) — задаёт параметры по умолчанию.  
> – **GREP_COLOR** и **GREP_COLORS** — управляют цветовой подсветкой.  
> – **LC_ALL**, **LC_CTYPE**, **LANG** — отвечают за кодировки и локализацию (например, регистр букв).