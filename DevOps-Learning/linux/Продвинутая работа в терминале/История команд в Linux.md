## ![](https://ucarecdn.stepik.net/f19029a0-4c1e-49dd-bef5-926f618ce7b1/)

## История команд в Linux

Когда вы работаете в терминале, Linux запоминает все введённые вами команды. Это называется **историей команд**, и она является важнейшим инструментом для эффективной и удобной работы в командной строке.

История команд позволяет вам быстро повторять, изменять и анализировать команды, которые вы использовали ранее, экономя время и упрощая работу с системой.

### Просмотр истории команд

Самый простой способ посмотреть историю команд — использовать команду:

```bash
history
```

Вы увидите список всех недавно использованных команд с порядковыми номерами, например:

```bash
  512  ls -la
  513  cd /var/www/html
  514  nano index.html
  515  sudo systemctl restart nginx
  516  git status
```

![](https://ucarecdn.stepik.net/28daa680-78bf-473d-a4a2-93deb35d3186/)

Чаще всего терминал хранит около 1000 последних команд, но это количество можно легко настроить.

### Повтор команд из истории

Вы можете легко повторить любую команду из истории, используя стрелки на клавиатуре или специальные комбинации:

- **Стрелка вверх (↑)** — предыдущая команда.
- **Стрелка вниз (↓)** — следующая команда.

Также можно выполнить конкретную команду из истории по её номеру, используя восклицательный знак (`!`):

```diff
!514
```

Это повторит команду под номером 514 (`nano index.html`).

### Быстрый поиск по истории

Для быстрого поиска команды, используйте сочетание клавиш `Ctrl + R` (обратный поиск в истории):

1. Нажмите `Ctrl + R` в терминале.
2. Начните набирать фрагмент команды, которую вы хотите найти.
3. Терминал предложит вам наиболее подходящий вариант.
4. Нажмите Enter, чтобы выполнить команду, или стрелку вправо, чтобы изменить её перед выполнением.

Пример:

```makefile
(reverse-i-search)`nano': nano index.html
```

📌 **Подсказка:** искать команды в истории можно разными способами:  
– сочетание клавиш Ctrl + R — поиск по подстроке в интерактивном режиме;  
– команда history | grep слово — поиск по истории через фильтр grep;  
– открыть файл ~/.bash_history любым текстовым редактором.

### Очистка истории команд

Если вы хотите очистить историю команд полностью, можно использовать команду:

```bash
history -c
```

Эта команда очистит текущую историю в сессии. Чтобы очистить файл истории, выполните также:

```bash
history -w
```

### Файл истории команд

Все введённые команды сохраняются в текстовом файле истории, расположенном по умолчанию в вашем домашнем каталоге:

- **Bash:** `~/.bash_history`
- **Zsh:** `~/.zsh_history`

Вы можете открыть и посмотреть этот файл любым текстовым редактором или командой:

```bash
cat ~/.bash_history
```

### Настройка размера истории

По умолчанию количество сохраняемых команд ограничено (обычно 1000 или 2000). Если вам нужно больше, вы можете изменить настройки, добавив в файл `~/.bashrc` строки:

```bash
export HISTSIZE=5000          # количество команд в памяти
export HISTFILESIZE=10000     # количество команд, сохраняемых в файле
```

Примените изменения:

```bash
source ~/.bashrc
```

### Игнорирование повторяющихся команд и команд с пробелом в начале

Если вы хотите, чтобы повторяющиеся команды или специфические команды (начинающиеся с пробела) не сохранялись в истории, используйте настройку `HISTCONTROL` в файле `~/.bashrc`:

```bash
export HISTCONTROL=ignoredups:ignorespace
```

- `ignoredups` — игнорировать повторяющиеся команды подряд.
- `ignorespace` — не сохранять команды, начинающиеся с пробела.

Теперь после применения изменений:

```bash
source ~/.bashrc
```

команды вида:

```bash
 ls -la
```

(с пробелом в начале) не попадут в историю.

📌 **Важно знать:** поведение истории можно настраивать через переменные окружения.  
– **HISTSIZE** — максимальное количество команд, хранимых в истории.  
– **HISTCONTROL=ignoredups** — не сохранять одинаковые команды подряд.  
– **HISTCONTROL=ignorespace** — не сохранять команды, которые начинаются с пробела.

### Использование истории для повторения команд с заменой

Полезная возможность — повторить предыдущую команду с заменой части текста:

- `^old^new^` — заменит «old» на «new» в последней команде и сразу выполнит её.

Пример:

```nginx
git checkout master
^master^develop^
```

Вторая команда быстро заменит `master` на `develop` и выполнит:

```nginx
git checkout develop
```

## Итог

**История команд** — незаменимый инструмент при работе в терминале Linux. Правильное использование истории позволяет вам экономить время, быстро находить нужные команды и избегать повторного набора одинаковых действий. Грамотно настроив параметры хранения и поиска по истории, вы значительно улучшите эффективность работы в командной строке.