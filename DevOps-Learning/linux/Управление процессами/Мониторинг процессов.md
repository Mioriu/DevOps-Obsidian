## ![](https://ucarecdn.stepik.net/e1ca64f6-7218-46f8-b7cd-7fc6081611eb/)

Мониторинг процессов — важнейшая задача для системного администратора Linux. В этом уроке мы подробно рассмотрим инструменты, с помощью которых вы сможете получать исчерпывающую информацию о работе процессов и состоянии вашей системы.

### Что показывает мониторинг процессов?

- Какие процессы запущены в данный момент.
- Какие ресурсы потребляет каждый процесс (CPU, память, диск, сеть).
- Состояния процессов и их активность в системе.
- Потенциальные проблемы: зависшие процессы, нехватку памяти, перегрузку процессора и дисковой подсистемы.

### ![](https://ucarecdn.stepik.net/80a7b71d-4b2f-436a-bc64-404d9f50708e/)

**ps (process status)** — базовая утилита для просмотра информации о процессах. Работает в статическом режиме (показывает текущий срез состояния процессов).

Наиболее часто используемые варианты команд:

- Просмотр всех процессов с полной информацией:
    
    ```dos
    ps aux
    ```
    
- Процессы конкретного пользователя:
    
    ```nginx
    ps -u username
    ```
    
- Процессы с указанием родительского PID:
    
    ```perl
    ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%cpu
    ```
    
- Поиск процессов по имени:
    
    ```perl
    ps aux | grep nginx
    ```
    

**Столбцы в выводе ps aux:**

- **USER** — владелец процесса
- **PID** — идентификатор процесса
- **%CPU** — загрузка процессора процессом
- **%MEM** — используемая оперативная память (%)
- **VSZ** — виртуальный размер памяти
- **RSS** — резидентный размер памяти (фактически используемая)
- **TTY** — терминал, связанный с процессом
- **STAT** — текущее состояние процесса
- **START** — время запуска процесса
- **TIME** — общее время работы процесса на CPU
- **COMMAND** — команда, запустившая процесс

_**📌 ps — основные команды**_

```perl
ps aux                # все процессы
ps -u user            # процессы пользователя
ps -f --ppid <PID>    # процессы с указанным PPID
ps aux | grep nano    # поиск процесса по имени
```

### ![](https://ucarecdn.stepik.net/87d14e47-3d3a-4935-a4b1-8cfecbfe7b90/)

**top** — интерактивная утилита, отображающая процессы в режиме реального времени. Позволяет отслеживать нагрузку системы.

Запуск:

```css
top
```

📌 Важные горячие клавиши в top:

- **M** — сортировать по памяти.
- **P** — сортировать по CPU (по умолчанию).
- **u** — показать процессы конкретного пользователя.
- **k** — завершить процесс (требует PID и сигнал).
- **c** — показать полные команды процессов.
- **q** — выйти из программы.

### ![](https://ucarecdn.stepik.net/bf973ead-9a6a-4611-92f9-0f679dd1b7e9/)

**htop** — более функциональный и удобный аналог `top`. Имеет улучшенный интерфейс и дополнительные возможности.

Установка:

- Debian/Ubuntu: `sudo apt install htop`
- CentOS/Fedora: `sudo dnf install htop`

Запуск:

```undefined
htop
```

📌 Горячие клавиши htop

- **FF3 — поиск процесса**
- **F6 — сортировка по критерию**
- **F9 — завершение процесса**

### ![](https://ucarecdn.stepik.net/c1d6c196-7be7-4a49-af81-f839c2ae902b/)

**pstree** показывает иерархию процессов в виде дерева. Удобно для анализа взаимосвязей и родительских процессов.

Использование:

```css
pstree -p -a
```

- `-p` — показать PID.
- `-a` — показать аргументы запуска процессов.

📌 pstree

```nginx
pstree       # дерево процессов
pstree -p    # дерево с PID
```

### ![](https://ucarecdn.stepik.net/91278d86-7fc5-4ec2-b1b9-36b09c79089a/)

**pidstat — утилита для CPU-статистики по процессам с интервалом!**

Установка (sysstat):

- Debian/Ubuntu: `sudo apt install sysstat`
- CentOS/Fedora: `sudo dnf install sysstat`

Использование (пример):

```css
pidstat -u -p PID 2 5
```

- `-u` — показать статистику CPU
- `-p PID` — мониторинг конкретного процесса
- `2 5` — интервал 2 сек, 5 раз

### ![](https://ucarecdn.stepik.net/452ff924-ef24-469e-9a71-2c9656374cea/)

**vmstat** — мониторинг памяти, CPU и активности ввода-вывода.

```nginx
vmstat 1 5
```

- `1` — интервал в секундах
- `5` — количество повторов

### ![](https://ucarecdn.stepik.net/93bad2b2-6b63-45cc-8ff0-07f2dc9bcb29/)

**iostat** — утилита для мониторинга активности дисковой подсистемы.

```nginx
iostat -xz 2 5
```

- `-x` — расширенная статистика дисков
- `-z` — скрыть неактивные устройства

### ![](https://ucarecdn.stepik.net/28eefc78-f59b-47f1-978e-8f4ae9bec7d8/)

**dstat** — универсальный инструмент мониторинга.

Установка:

- Debian/Ubuntu: `sudo apt install dstat`

Использование:

```nginx
dstat -cdlmnpsy 1 10
```

### ![](https://ucarecdn.stepik.net/113e9ad0-d075-44dd-b7cc-a6dd18761cf0/)

**atop** — мощный инструмент мониторинга с логированием и анализом истории.

Установка:

- Debian/Ubuntu: `sudo apt install atop`
- CentOS/Fedora: `sudo dnf install atop`

Запуск:

```undefined
atop
```

> ### ⚡ Дополнительно  
> Эти утилиты нужны для глубокого анализа:  
> pidstat  — статистика CPU по процессам  
> vmstat   — память и ввод-вывод  
> iostat   — нагрузка на диски  
> dstat    — комплексная статистика (CPU, диски, сеть)  
> atop     — мониторинг с логированием

### ✅ Главное запомнить

- ps, pstree — быстрый просмотр процессов
    
- top, htop  — мониторинг в реальном времени
    
- Остальные утилиты (pidstat, vmstat, iostat, dstat, atop) пригодятся для анализа сложных ситуаций.
    

Теперь вы знаете все основные инструменты мониторинга процессов в Linux и готовы эффективно использовать их для анализа и поддержки работоспособности вашей системы.