## ![](https://ucarecdn.stepik.net/54e07ed5-55fb-4893-a466-2eb52d3e1994/)

В основе работы любой операционной системы лежит понятие **процесса**. Для грамотного администрирования Linux необходимо понимать, что такое процессы, как они создаются, запускаются и взаимодействуют друг с другом. В этом уроке мы подробно разберём основы процессов Linux, не затрагивая более продвинутые темы, которые будут рассмотрены позже.

### Что такое процесс?

**Процесс** — это запущенная программа или её экземпляр, который выполняется в операционной системе. Каждый процесс имеет:

- Уникальный идентификатор (PID — Process ID)
- Состояние (выполняется, спит, остановлен и т.д.)
- Собственную выделенную память и ресурсы
- Связь с родительским процессом (PPID — Parent Process ID)

Когда вы запускаете любую программу в терминале или через интерфейс, система создает новый процесс.

### Идентификаторы процессов (PID и PPID)

Каждый процесс в Linux получает уникальный числовой идентификатор — **PID**. Эти номера выдаются последовательно по мере запуска новых процессов.

Кроме PID у каждого процесса есть родительский идентификатор — **PPID**. Родительский процесс — это тот, который породил текущий процесс.

Посмотреть PID и PPID процессов можно командой `ps`:

```nginx
ps -ef
```

- `-e` — показывает все процессы системы
- `-f` — подробный формат с PID, PPID и дополнительной информацией
- `-a` — все процессы, кроме сессии терминала;
    
- `-x` — процессы без управляющего терминала.
    

### Жизненный цикл процесса

В Linux процессы могут находиться в следующих состояниях:  

- - running — выполняется;  
- - sleeping — ждёт события или ресурса;  
- - stopped — остановлен;  
- - zombie — завершён, но запись ещё есть в таблице процессов.  

В Linux процессы могут находиться в состояниях: running, sleeping, stopped, zombie. В этом уроке мы используем упрощённое описание, подробнее будет в продвинутых темах

### Основные атрибуты процессов

У каждого процесса в Linux есть ряд ключевых атрибутов:

- **PID** — уникальный идентификатор процесса
- **PPID** — идентификатор родительского процесса
- **UID** — идентификатор пользователя, запустившего процесс
- **GID** — идентификатор группы пользователя
- **Приоритет** — влияет на очередность выполнения процесса (будет подробно рассмотрен отдельно)
- **TTY (терминал)** — идентификатор терминала, в котором был запущен процесс
- **Команда** — название программы или команды, породившей процесс

### Просмотр и идентификация процессов

В Linux существуют базовые команды для просмотра информации о процессах.

- `ps` — базовая команда для просмотра списка процессов:
    
    ```dos
    ps aux
    ```
    
    Покажет подробную информацию обо всех процессах.
- `pstree` — показывает дерево процессов и их иерархию:
    
    ```css
    pstree -p
    ```
    
    Опция `-p` добавляет PID к каждому процессу.
- `top` — интерактивная утилита мониторинга процессов:
    
    ```css
    top
    ```
    
    Позволяет видеть процессы в реальном времени и их нагрузку на систему.

👉 «Чтобы найти конкретный процесс, удобно использовать комбинацию `ps aux | grep <имя_процесса>`».

Подробное изучение инструментов мониторинга будет в отдельном уроке.

### Создание новых процессов

При запуске любой команды в терминале Linux автоматически создает новый процесс. Например, при запуске простого текстового редактора `nano`:

```nginx
nano testfile.txt
```

Система немедленно выделяет процессу PID, который можно увидеть, открыв другой терминал и выполнив:

```perl
ps aux | grep nano
```

### Родительские и дочерние процессы

В Linux процессы часто имеют родительско-дочернюю иерархию. Родительский процесс (parent) порождает дочерний процесс (child). Например, когда вы запускаете команду из bash, bash становится родительским процессом для запускаемой команды.

Посмотреть иерархию процессов можно с помощью команды `pstree`:

```ruby
pstree -ps $$
```

- `$$` — PID текущего shell (терминала)
- Опция `-s` отображает родительские процессы до корня системы

### Завершение процессов

Самый простой способ завершить процесс — нажать сочетание клавиш `Ctrl+C`, если процесс запущен в текущем терминале.

Другой вариант — команда `kill`, с указанием PID:

```bash
kill PID
```

Подробное изучение сигналов и завершения процессов будет рассмотрено отдельно, здесь мы даем только базовые принципы.