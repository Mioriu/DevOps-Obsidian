## ![](https://ucarecdn.stepik.net/1c055a59-bdb9-4565-a096-f0010d5d1151/)

Важный показатель производительности и загрузки Linux-системы — это **Load Average**. В этом уроке подробно разберём, что он означает, как его интерпретировать, и как использовать на практике.

### Что такое Load Average?

**Load Average** — это среднее количество процессов, которые находятся в состоянии ожидания ресурсов (в основном процессора и дисковой подсистемы) или активно выполняются на процессоре в конкретный момент времени.  
Очень простое объяснение `la` мы дали в [**этом посте**](https://t.me/devops_prosto/74).

Представьте, что у вас одно рабочее место (1 CPU). Если очередь из задач = 1, всё нормально. Если задач = 2, значит одна работает, вторая ждёт. Для 4 рабочих мест (4 CPU) нормальная очередь = 4.

Значение Load Average выводится в виде трёх чисел, которые показывают среднюю нагрузку за:

- последнюю 1 минуту;
- последние 5 минут;
- последние 15 минут.

Например, если вы увидите такие цифры:

```lua
load average: 0.50, 0.80, 1.20
```

Это значит:

- Средняя нагрузка за последнюю минуту: 0.50
- Средняя нагрузка за 5 минут: 0.80
- Средняя нагрузка за 15 минут: 1.20

### Как узнать Load Average?

Вы можете увидеть текущий Load Average, используя несколько команд:

- **uptime**:
    
    ```bash
    uptime
    ```
    
- **top** или **htop** (в верхней части экрана):
    
    ```css
    top
    ```
    
- **cat /proc/loadavg**:
- ```bash
    cat /proc/loadavg
    ```
    

Чаще всего для проверки используют простую команду `uptime` — в выводе вы сразу увидите и время работы сервера, и нагрузку.

### Что означают числа Load Average?

Чтобы правильно понять Load Average, важно знать количество процессорных ядер вашей системы:

```nginx
grep -c ^processor /proc/cpuinfo
```

Если ваша система имеет одно ядро (CPU):

- Load Average **ниже 1.0** — ресурсы CPU используются не полностью, система не перегружена.
- Load Average **равен 1.0** — ресурсы CPU используются ровно на 100%, все процессы справляются.
- Load Average **выше 1.0** — ресурсы CPU не успевают обрабатывать все процессы сразу, появляется очередь ожидания.

Для многопроцессорных (многоядерных) систем правило аналогичное, но с поправкой на число ядер. Например, если у вас 4 ядра CPU:

- Load Average **ниже 4.0** — нормальная загрузка.
- Load Average **около 4.0** — высокая загрузка, ресурсы используются полностью.
- Load Average **выше 4.0** — процессоры не успевают обрабатывать задачи, процессы начинают ожидать в очереди.

**Пример:**

- Для сервера с 2 ядрами нормой будет Load Average около 2.
    
- Если LA = 4, то система работает в 2 раза медленнее ожидаемого, так как половина процессов стоит в очереди.
    

### Интерпретация динамики Load Average

Сравнивая показатели за 1, 5 и 15 минут, можно понять, как меняется загрузка системы:

- Если Load Average за 1 минуту выше, чем за 5 и 15 минут (например, `2.5, 1.0, 0.5`), нагрузка быстро растёт прямо сейчас.
- Если Load Average за 15 минут выше текущего (например, `0.5, 1.0, 1.5`), нагрузка снижается.
- Если числа примерно равны (например, `1.0, 1.1, 1.0`), нагрузка стабильна.

```1c
1 минута ↑ больше остальных → нагрузка растёт
15 минут ↑ больше → нагрузка падает
Все примерно равны → нагрузка стабильна
```

### Причины высокого Load Average

Высокий Load Average может быть вызван:

- Высокой загрузкой CPU (процессорной нагрузки).
- Ожиданием ввода-вывода (особенно с медленными дисками или сетевыми файловыми системами).
- Дефицитом памяти и активным использованием файла подкачки (swap).
- Большим количеством активных процессов одновременно.

> Самые частые причины на практике:
> 
> - слишком тяжёлый процесс (например, база данных);
>     
> - активная работа с диском (копирование, бэкап);
>     
> - нехватка RAM → система уходит в swap.
>     

👉 Если вы не знаете, с чего начать → просто откройте `top` и нажмите `M` (по памяти) или `P` (по CPU). В 90% случаев это сразу покажет виновника

### Как понять, почему вырос Load Average?

Если вы увидели резкое повышение Load Average, выполните следующие шаги:

1. Проверьте процессы с помощью `top` или `htop`, отсортировав их по загрузке CPU (клавиша P) или памяти (клавиша M).
2. Используйте `iostat -xz 1 5`, чтобы проверить нагрузку на диски.
3. Используйте `vmstat 1 5`, чтобы увидеть нагрузку на память и активность swap.
4. Проверьте логи системы (`/var/log/syslog`, `/var/log/messages`) на предмет ошибок или предупреждений.

### Типичные примеры интерпретации Load Average

- `0.20, 0.30, 0.25` — низкая стабильная нагрузка, проблем нет.
- `1.50, 0.80, 0.40` — резкий всплеск нагрузки в последнюю минуту, нужно проверить активность процессов сейчас.
- `3.50, 3.80, 3.70` на 4-ядерном CPU — стабильная, но высокая нагрузка, близкая к пределу.
- `8.00, 7.50, 7.20` на 4-ядерном CPU — серьёзная перегрузка системы, процессы сильно тормозят, требуется срочная диагностика и устранение причины.

### Как снизить Load Average?

Способы снижения нагрузки зависят от причины:

- Уменьшение количества запущенных процессов.
- Оптимизация приложений, которые нагружают процессор или память.
- Использование более быстрого диска (например, SSD вместо HDD).
- Добавление оперативной памяти или расширение ресурсов CPU (масштабирование).
- Разделение задач на разные серверы, если ресурсы постоянно загружены на пределе.

Теперь вы понимаете, что такое Load Average и как его использовать для оценки текущей загрузки и производительности Linux-системы.