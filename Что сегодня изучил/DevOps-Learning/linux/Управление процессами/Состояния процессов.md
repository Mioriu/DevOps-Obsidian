## ![](https://ucarecdn.stepik.net/7450a53d-a28d-44b5-9677-99e67e651b5f/)

В Linux каждый процесс постоянно меняет свои состояния в зависимости от того, что происходит внутри системы и самого приложения. Глубокое понимание всех возможных состояний позволяет эффективно диагностировать проблемы, оптимизировать работу системы и профессионально управлять процессами.

### Полный перечень состояний процессов в Linux

Рассмотрим детально все возможные состояния процессов в Linux:

> R — выполняется (running)    
> S — прерываемое ожидание (sleeping)    
> D — непрерываемое ожидание (disk/uninterruptible sleep)    
> T — остановлен (stopped)    
> Z — зомби (zombie)    
> I — idle (для потоков ядра)

👉 Основные состояния: **R, S, D, T, Z**.  
👉 Дополнительно: сироты (Orphaned) и Idle (ядро).

### ![](https://ucarecdn.stepik.net/78e88617-875b-42aa-af3d-ecfa70b493e1/)

Процесс в состоянии Running либо в данный момент выполняется на процессоре, либо полностью готов это сделать и ожидает своей очереди в планировщике процессов.

**Пример:** запущенная команда `grep` или `ls` обычно находится в состоянии Running во время выполнения.

### ![](https://ucarecdn.stepik.net/9a0de111-cd79-40a5-ae11-fa095a6f8c4a/)

Состояние Sleeping (также называют Interruptible sleep) означает, что процесс временно приостановлен и ожидает некоторого события, например:

- получения данных от устройства ввода-вывода;
- таймера;
- сигнала от другого процесса или пользователя.

Такие процессы легко «просыпаются» при наступлении ожидаемого события или получении сигнала от системы.

**Пример:** демоны, ожидающие сетевых подключений (например, веб-сервер Apache).

### ![](https://ucarecdn.stepik.net/c2ee3140-c84a-4bec-96b7-0277029abce5/)

Это специальное состояние ожидания, когда процесс выполняет операцию ввода-вывода с диском, и она не может быть прервана обычными сигналами (например, SIGKILL). Процесс обязательно дождётся окончания этой операции. Длительное пребывание процесса в состоянии Uninterruptible Sleep может указывать на проблемы с аппаратурой (диски, RAID-контроллеры) или файловой системой.

**Пример:** копирование больших объёмов данных на диск.

### ![](https://ucarecdn.stepik.net/7c7e5e6d-8eef-4085-b66e-ce51928232d3/)

Процесс переходит в состояние Stopped, если он был явно остановлен пользователем или сигналом `SIGSTOP`. Остановленные процессы не получают время процессора до тех пор, пока не будет отправлен сигнал продолжения (`SIGCONT`).

**Пример:** нажатие `Ctrl+Z` в терминале останавливает текущий процесс.

Возобновить процесс можно командой:

```bash
kill -CONT PID
```

### ![](https://ucarecdn.stepik.net/24555aaf-69e5-4c6c-a746-a57f48ad8e70/)

Процесс-зомби — это завершённый процесс, который ещё числится в таблице процессов, поскольку его родительский процесс не забрал результат его выполнения (статус завершения). Такие процессы не потребляют вычислительные ресурсы, но занимают место в таблице процессов.

Чтобы избавиться от зомби, нужно завершить или перезапустить родительский процесс.

Проверка на наличие процессов-зомби:

```perl
ps aux | grep ' Z'
```

### ![](https://ucarecdn.stepik.net/1fac406c-077e-4d6d-ac5c-38805514a042/)

Процесс-сирота возникает, когда родительский процесс завершился раньше дочернего. В таком случае дочерний процесс «усыновляется» процессом `init` (с PID 1 или `systemd` в современных дистрибутивах). Сиротские процессы продолжают нормальную работу под новым родителем и не являются проблемой.

Проверка процесса-сироты (PPID = 1):

```nginx
ps -eo pid,ppid,cmd | awk '$2==1'
```

### ![](https://ucarecdn.stepik.net/22be18a4-a3d6-4526-9011-b05a35d4a803/)

Состояние Idle используется преимущественно внутренними потоками ядра Linux. Эти потоки находятся в режиме ожидания выполнения задач планировщика и не нагружают систему, когда неактивны. В пользовательском пространстве это состояние встречается редко.

### ![](https://ucarecdn.stepik.net/90cfb70d-336d-4a90-b451-f5401e6f1c66/)

В выводе команд `ps` или `top` состояние процесса может дополняться различными буквами и символами:

- `<` — Высокий приоритет процесса
- `N` — Низкий приоритет процесса (запущен с командой nice)
- `+` — Процесс в foreground-группе
- `s` — Лидер сессии (session leader)
- `l` — Многопоточный процесс (multi-threaded)
- `L` — Процесс имеет страницы, заблокированные в памяти (locked)

_Чаще всего состояния удобно смотреть через команды:_

- `ps -eo pid,state,cmd` — только PID, состояние и команду.
    
- `top` или `htop` — в реальном времени.
    

⚠️ **На что обратить внимание:**

- `D` — если процесс «завис» в этом состоянии → проверь диски или файловую систему.
    
- `Z` — процессы-зомби → ищи проблему у родительского процесса.
    
- `T` — процесс остановлен пользователем (например, Ctrl+Z).
    

### Просмотр состояния процессов

Для просмотра текущих состояний процессов удобно использовать команды:

- `ps aux` — покажет все процессы и их состояния.
- `top` или `htop` — интерактивный мониторинг в реальном времени.
- `ps -eo pid,state,cmd` — краткий просмотр состояний всех процессов.

**Пример вывода `ps -eo pid,state,cmd`:**

```swift
PID S CMD
  1 S /sbin/init
243 S /usr/lib/systemd/systemd-journald
954 R ps -eo pid,state,cmd
123 Z [defunct_process]
204 T vim example.txt
```

### Когда состояния процессов требуют внимания?

- Если процесс постоянно находится в состоянии **D (uninterruptible sleep)**, стоит проверить состояние оборудования (дисковая подсистема).
- Большое количество процессов-зомби (Z) может свидетельствовать об ошибках в приложениях.
- Появление множества сиротских процессов может быть нормальным, но иногда сигнализирует о неправильном завершении родительских процессов.

Теперь вы детально знаете все возможные состояния процессов в Linux и сможете быстро выявить и исправить потенциальные проблемы с производительностью и стабильностью системы.